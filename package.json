{
  "name": "vi-email",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.1",
    "@twilio/runtime-handler": "1.3.0",
    "twilio": "5.0.1"
  },
  "devDependencies": {
    "twilio-run": "^3.5.4"
  },
  "engines": {
    "node": "18"
  }
}
