{
  "name": "vi-email",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.56",
    "@twilio/runtime-handler": "1.3.0"
  },
  "devDependencies": {
    "twilio-run": "^3.5.4"
  },
  "engines": {
    "node": "18"
  }
}
